# the path to the Unity executable to use
unity_location: "D:/Programs/UnityEditors/2017.4.30f1/Editor/Unity.exe"

# the method to execute when Unity is launched
execute_method: "LSDR.Editor.BuildSystem.BuildScript.Build"

# the path to the project to open
project_path: "D:/Documents/git-repos/LSDRevamped/LSDR"

# the build defs file within the project_path
build_defs_path: "build_defs.json"

# the path within the project_path to store releases in
releases_path: "releases/"